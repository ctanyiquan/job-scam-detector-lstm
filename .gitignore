# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
*.egg-info/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/
__pycache__/

# ---------------------------------------------------------------------------
# Virtual environments
# ---------------------------------------------------------------------------
.env
.venv/
env/
venv/

# ---------------------------------------------------------------------------
# Data
#   Raw and generated data are not committed (large / re-creatable).
# ---------------------------------------------------------------------------
data/processed/
data/raw/

# ---------------------------------------------------------------------------
# Local-only planning & assistant docs (not published to GitHub)
#   These are working documents for the team, shared outside of git.
#   CLAUDE.md must stay at the repo root to be auto-loaded, so it is
#   ignored by name rather than by folder.
# ---------------------------------------------------------------------------
CLAUDE.md
planning/

# ---------------------------------------------------------------------------
# Models
#   The LSTM candidate is regenerated by re-running 05_modelling_lstm.ipynb
#   (training is stochastic, so it's never a stable, committable artifact).
#   The final deployed_pipeline.joblib from 06_evaluation.ipynb IS committed.
# ---------------------------------------------------------------------------
models/lstm_candidate.joblib

# ---------------------------------------------------------------------------
# Results
#   Analysis outputs exported from notebooks (for example the deployed
#   model's vocabulary CSV from 06_evaluation.ipynb). Regenerated by
#   re-running the notebook, so not committed, the same as data/processed/.
# ---------------------------------------------------------------------------
results/

# ---------------------------------------------------------------------------
# Editor / OS
# ---------------------------------------------------------------------------
.DS_Store
.idea/
.vscode/
Thumbs.db
